-- Print type after each evaluation
:set +t

-- Show loaded modules in the prompt
-- :set prompt "%s> "
:set prompt "> "

-- Fix the printing of Unicode characters
import System.IO qualified as SI
SI.hSetEncoding SI.stdout SI.utf8
