#!/bin/sh

set -o errexit
set -o xtrace

mainline=main
if ! git branch | grep --fixed "$mainline" > /dev/null; then
    mainline=master
fi

git checkout "$mainline"
git pull
git remote prune origin
git branch --merged | grep -v --fixed "* ${mainline}" | xargs git branch -d
