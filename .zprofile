# This script is sourced by zsh it it is run as a "login" shell.
#
# Since I'm using a graphical login, lightdm will invoke one such to prepare
# the environment in which the X session runs.

# Start SSH agent
eval `ssh-agent`
